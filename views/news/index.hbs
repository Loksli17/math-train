<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>News</title>
    <style>
        .pages{
            margin-top: 30px;
        }

        .pages ul{
            display: flex;
        }

        .pages li{
            width: 40px;
            height: 40px;
            list-style: none;
            margin-left: 10px;
        }

        .pages .page{
            background: #fff;
            width: 100%;
            height: 100%;
            text-align: center;
            line-height: 40px;
            display: block;
            color: #cc33cc;
            border: 1.5px solid;
            text-decoration: none;
            font-size: 18px;
        }

        .pages .active{
            background: #cc33cc;
            border: 1.5px solid #cc33cc;
            color: #fff;
        }

    </style>
</head>
<body>
{{#each news}}
    <a href="/news/new?id={{this._id}}"> id: {{this._id}} title: {{this.title}}  date: {{this.date.date}} time {{this.date.time}}</a>  <br>
{{/each}}

<div class="pages">
    <ul>
        {{#each pages}}
            <li>
                {{#if current}}
                    <a class="active page" href="{{route}}page={{id}}">{{content}}</a>
                {{else}}
                    <a class="page" href="{{route}}page={{id}}">{{content}}</a>
                {{/if}}
            </li>
        {{/each}}
    </ul>
</div>
</body>
</html>